# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
!arckit-paperclip/src/lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# Virtual environments
venv/
ENV/
env/
.venv

# Node
node_modules/

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/

# Project specific
*.log
.arckit-debug/
research/

# Plugin runtime caches — ephemeral, project-scoped, regenerated each session
# (see #494 close: these belong in .gitignore, not ${CLAUDE_PLUGIN_DATA})
.arckit/memory/.last-session
.arckit/memory/.telemetry.jsonl
.arckit/memory/.cc-version
docs/telemetry.json

# Projects (created by arckit init, not part of the toolkit itself)
projects/

# Vendor proposals (may contain sensitive info)
# Uncomment if you don't want to commit vendor documents
# projects/*/vendors/*/proposal.pdf
# projects/*/vendors/*/proprietary/

# Legacy local directories (no longer generated)
.codex/
.opencode/
.agents/skills/
.claude/worktrees/


# ── Generated distribution formats ────────────────────────────────────────────
# arckit-claude is the single source of truth. The non-Claude extension formats
# below are regenerated by scripts/converter.py (commands/prompts/agents/skills/
# templates/docs-guides/config/schemas/references/scripts/hooks) and published to
# their own GitHub repos by scripts/push-extensions.sh, which tars the working
# tree from disk (not git), so generated content does NOT need to be tracked.
# Only hand-authored files are tracked here: README/VERSION/LICENSE, the platform
# manifests (config.toml, opencode.json, gemini-extension.json, package.json),
# hand-maintained hooks (codex .mjs, gemini hooks/scripts/*.py), paperclip TS
# source, and each docs/DEPENDENCY-MATRIX.md. Regenerate locally before release.
# See CLAUDE.md "Generated extension formats are not tracked".

# -- arckit-codex (generated) --
arckit-codex/commands/
arckit-codex/prompts/
arckit-codex/agents/
arckit-codex/skills/
arckit-codex/templates/
arckit-codex/docs/guides/
arckit-codex/config/
arckit-codex/schemas/
arckit-codex/references/
arckit-codex/scripts/
arckit-codex/.codex-plugin/
arckit-codex/config.toml
arckit-codex/.mcp.json
arckit-codex/hooks/owm-tidy.mjs
arckit-codex/hooks/wardley-label-placement.mjs

# -- arckit-opencode (generated) --
arckit-opencode/commands/
arckit-opencode/agents/
arckit-opencode/skills/
arckit-opencode/templates/
arckit-opencode/docs/guides/
arckit-opencode/config/
arckit-opencode/schemas/
arckit-opencode/references/
arckit-opencode/scripts/
arckit-opencode/hooks/

# -- arckit-copilot (generated) --
arckit-copilot/prompts/
arckit-copilot/agents/
arckit-copilot/skills/
arckit-copilot/templates/
arckit-copilot/docs/guides/
arckit-copilot/config/
arckit-copilot/schemas/
arckit-copilot/references/
arckit-copilot/scripts/
arckit-copilot/hooks/
arckit-copilot/copilot-instructions.md

# -- arckit-gemini (generated; hooks/scripts/*.py are hand-maintained, kept) --
arckit-gemini/commands/
arckit-gemini/templates/
arckit-gemini/scripts/
arckit-gemini/skills/
arckit-gemini/docs/
arckit-gemini/agents/
arckit-gemini/config/
arckit-gemini/policies/
arckit-gemini/references/
arckit-gemini/schemas/
arckit-gemini/hooks/hooks.json
arckit-gemini/hooks/owm-tidy.mjs
arckit-gemini/hooks/wardley-label-placement.mjs

# -- arckit-paperclip (generated; hand-written TS in src/ is kept) --
arckit-paperclip/templates/
arckit-paperclip/docs/
arckit-paperclip/skills/
arckit-paperclip/config/
arckit-paperclip/schemas/
arckit-paperclip/references/
arckit-paperclip/hooks/
arckit-paperclip/src/data/*
!arckit-paperclip/src/data/.gitkeep

# Draft articles (not for version control)
docs/articles/
.tmp*

# Superpowers process artefacts (brainstorming specs + implementation plans).
# Dev-workflow scratch, not part of the published toolkit; kept locally only.
docs/superpowers/

# Local investor / pitch drafts (kept off the published toolkit; tracked
# docs/INVESTOR-REPORT.md is the single shared artefact)
docs/BUSINESS-PLAN.md
docs/INVESTOR-DECK.*
docs/INVESTOR-REPORT-hero.*

# Git worktrees
.worktrees/

# Test output
tests/*/output/

# Local experiments — not part of the published toolkit
tests/mermaid-wardley/
.claude/scheduled_tasks.lock

# tools/wardley-lsp/ is a clone of the separate (private) tractorjuice/wardley-lsp
# repo (the OnlineWardleyMaps Language Server). Same convention as
# arckit-consulting below: changes belong in that repo, not here.
tools/wardley-lsp/

# Ignyte scraper - captured session token (never commit)
docs/auth.txt

# Investor pitch decks - confidential fundraising material.
# arc-kit is a public repo; the decks, scraped investor data (with PII),
# scraper config/tokens, and generator scripts must never be committed.
docs/pitch-decks/
docs/2026-05-20-difc-innovation-licence-setup-guide.pdf

# Partnership/funding proposals - confidential business material, kept off the
# public repo (same rationale as pitch-decks above).
docs/proposals/

# LinkedIn data export - contains personal PII (connections, messages, phone)
docs/*LinkedInDataExport*

# arckit-consulting/ is a clone of the separate tractorjuice/arckit-fde repo.
# Per project memory, FDE-site changes belong in that repo, not here.
arckit-consulting/
