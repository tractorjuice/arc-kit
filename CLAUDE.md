# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Overview

ArcKit is **The Enterprise Architecture Governance Harness** — providing slash commands for AI coding assistants (Claude Code, Codex CLI, Gemini CLI, OpenCode CLI, GitHub Copilot) to generate strategy, architecture, delivery, and assurance artifacts. It transforms architecture governance from scattered documents into a systematic, template-driven process.

**Nine distribution formats** exist side-by-side in this repo:

1. **CLI package** (`src/arckit_cli/`) — Python CLI installed via `pip`/`uv`, runs `arckit init` to scaffold projects for Codex CLI or OpenCode CLI
2. **Claude Code plugins** (`plugins/arckit-claude/`, `plugins/arckit-uae/`, `plugins/arckit-fr/`, `plugins/arckit-ca/`, `plugins/arckit-eu/`, `plugins/arckit-at/`, `plugins/arckit-au/`, `plugins/arckit-us/`, `plugins/arckit-uk-finance/`, `plugins/arckit-fde/`) — installed via marketplace (`/plugin marketplace add tractorjuice/arckit-claude`). **Use that marketplace, not `tractorjuice/arc-kit`.** Both publish all 16 plugins under identical names, so adding both registers `arckit`, `arckit-repo` and the rest twice and leaves the plugin manager choosing between ambiguous entries (reported on #680). Core `arckit` ships 75 commands + all hooks/MCP/doc-types config. Eight community overlays ship commands and recipes — seven jurisdictional overlays (UAE, France, Canada, EU, Austria, Australia, USA federal civilian) plus one sector-specific overlay (UK Finance Payments). Install only what you need; community plugins require the `arckit` core plugin. One **tooling plugin** — `arckit-fde` — ships separately: a lean, Claude Code only plugin with one command, `/arckit-fde:create`, that generates a brandable white-label Forward Deploy Engineering consulting website into `docs/`; no dependencies, not in the converter, no governance doc-types.
3. **Gemini CLI extension** (`extensions/arckit-gemini/`) — published as `tractorjuice/arckit-gemini`, installed via `gemini extensions install`
4. **OpenCode CLI extension** (`extensions/arckit-opencode/`) — scaffolded via `arckit init --ai opencode`
5. **Codex CLI extension** (`extensions/arckit-codex/`) — published as `tractorjuice/arckit-codex`
6. **Copilot extension** (`extensions/arckit-copilot/`) — prompt files + custom agents for GitHub Copilot, scaffolded via `arckit init --ai copilot`
7. **Paperclip extension** (`extensions/arckit-paperclip/`) — published as `tractorjuice/arckit-paperclip`, installed via Paperclip's plugin installation flow
8. **Mistral Vibe CLI extension** (`extensions/arckit-vibe/`) — published as `tractorjuice/arckit-vibe`, linked into `~/.vibe/extensions/`
9. **Kimi Code CLI extension** (`extensions/arckit-kimi/`) — published as `tractorjuice/arckit-kimi`, installed from inside the Kimi Code TUI with `/plugins install https://github.com/tractorjuice/arckit-kimi.git`. Commands ship as Agent Skills (`skills/<name>/SKILL.md`) invoked with `/skill:arckit-<command>`; the `kimi.plugin.json` manifest carries the six MCP servers and auto-loads `architecture-workflow` at session start.

The CLI and plugin have independent version numbers; all extensions track the plugin version. Non-Claude formats are generated by `scripts/converter.py` from the plugin source. Claude Code support was removed from the CLI in favour of the plugin, which references files in-place via `${CLAUDE_PLUGIN_ROOT}`.

**Repo layout:** the 11 Claude Code plugin source directories live under `plugins/` (e.g. `plugins/arckit-claude/`); `marketplace.json` at the repo root points each entry at `./plugins/<name>`. The generated, gitignored non-Claude extension dirs (`extensions/arckit-codex/`, `extensions/arckit-gemini/`, `extensions/arckit-opencode/`, `extensions/arckit-copilot/`, `extensions/arckit-paperclip/`, `extensions/arckit-vibe/`, `extensions/arckit-kimi/`) live under `extensions/` — they are converter outputs, not source.

**Generated extension formats are not tracked.** `extensions/arckit-codex/`, `extensions/arckit-opencode/`, `extensions/arckit-copilot/`, `extensions/arckit-gemini/`, `extensions/arckit-paperclip/`, `extensions/arckit-vibe/`, and `extensions/arckit-kimi/` are regenerated wholesale from `arckit-claude` (plus the community overlays) by `scripts/converter.py` — commands/prompts, agents, skills, templates, `docs/guides`, config, schemas, references, scripts, and the converter-built hooks. That generated content is **gitignored** (see `.gitignore` → "Generated distribution formats"); only the hand-authored files are tracked per extension: `README.md` / `VERSION` / `LICENSE`, the platform manifests (`config.toml`, `opencode.json`, `gemini-extension.json`, `package.json`/`tsconfig.json`), hand-maintained hooks (codex `hooks/*.mjs` including `sync-guides.mjs`, gemini `hooks/scripts/*.py`), the paperclip TypeScript source under `src/` (except generated `src/data/`), and each `docs/DEPENDENCY-MATRIX.md`. `arckit-claude` is the single source of truth. **Run `python scripts/converter.py` after pulling** so the extension dirs exist locally; `scripts/push-extensions.sh` tars the working tree (not git) into each extension's own published repo, so the generated files reach those repos at release time without being committed here. CI's `codex-plugin.yml` regenerates before running `tests/codex/`.

## Build & Development Commands

```bash
# Install in development mode (uv recommended)
uv pip install -e .

# Test CLI
arckit --help
arckit check

# Test project initialization
arckit init test-project --ai codex --no-git
arckit init test-project --ai codex --minimal  # Skip docs/guides

# Build distribution
python -m build

# Convert plugin to Codex/OpenCode/Gemini/Copilot formats
python scripts/converter.py

# Lint markdown files
npx markdownlint-cli2 "**/*.md"
npx markdownlint-cli2 --fix "**/*.md"
```

## Architecture

### Core Components

**CLI Entry Point** (`src/arckit_cli/__init__.py`):

- Single-file CLI using `typer`. `arckit init` scaffolds project structure and copies templates/commands from package data. `get_data_paths()` resolves data across install methods (uv tools, pip, source).

**Package Data** (distributed via `pyproject.toml` shared-data): `.codex/prompts/`, `.opencode/commands/`, `.arckit/templates/`, `scripts/bash/`, `docs/guides/`, plus `docs/{README,DEPENDENCY-MATRIX,WORKFLOW-DIAGRAMS}.md`.

**Data Path Resolution Order**: source/dev mode → uv tool install → pip install → platformdirs → source fallback.

### Slash Command System

Commands exist in multiple formats across the distribution. The plugin source is canonical; the converter generates the rest.

| AI | Format | Location |
|----|--------|----------|
| Claude Code | `/arckit:requirements` (plugin) | `plugins/arckit-claude/commands/requirements.md` |
| Codex CLI | `$arckit-requirements` (skill) | `.agents/skills/arckit-requirements/SKILL.md` |
| Gemini CLI | `/arckit:requirements` (extension) | `extensions/arckit-gemini/commands/arckit/requirements.toml` |
| OpenCode CLI | `/arckit:requirements` | `.opencode/commands/arckit.requirements.md` |
| Copilot | `/arckit-requirements` (prompt) | `extensions/arckit-copilot/prompts/arckit-requirements.prompt.md` |
| Kimi Code CLI | `/skill:arckit-requirements` (skill) | `extensions/arckit-kimi/skills/arckit-requirements/SKILL.md` |

**Plugin command frontmatter** supports:

- `description` (required)
- `doc-type:` (required) — the doc-type code the command writes, as data. A single code (`doc-type: REQ`), a `[A, B]` list where a command writes more than one governed artefact, or `none` where it writes no `ARC-*` artefact at all (20 commands, e.g. `/arckit:search`, `/arckit:health`, `/arckit:pages`). It sits on the **command** even when the command delegates to an agent that holds the Write call (`/arckit:framework` declares `FWRK`): the declaration describes what running the command produces, which is what a recipe target names. `check-doc-type-registry.py` asserts every recipe `output.type` equals it. Before this existed the gate could only tell whether a code *resolved*, not whether it named the *right* artefact — `UAE-PROC` was one plausible fix away from `PROC`, which is registered to Canada's Federal Procurement Strategy (#715)
- `effort:` (override session effort — frontmatter accepts `low`, `medium`, `high`, `xhigh`, `max`. Per-model support, from the [model-config docs](https://code.claude.com/docs/en/model-config#adjust-effort-level): **Opus 5, Sonnet 5, Opus 4.8, Opus 4.7, Fable 5 and Fable 5.1 support all five**; Opus 4.6 and Sonnet 4.6 support `low`/`medium`/`high`/`max` but **not** `xhigh`. An unsupported level falls back to the highest supported level at or below it (e.g. `xhigh` → `high` on Opus 4.6). `max` is the deepest tier (above `xhigh`) but the docs caution it can overthink — test before adopting broadly. Default effort is `high` on every model that supports effort, except Opus 4.7 which defaults to `xhigh`. ArcKit's heavy commands/agents use `effort: max`, which is valid in frontmatter and runs as `max` on Opus 5 while thinking is on. From Claude Code v2.1.251 an Opus 5 request at `xhigh`/`max` with thinking disabled is sent as `high` instead of failing — a runtime downgrade that `provenance-stamp.mjs` cannot observe, so Effective Effort over-reports on thinking-off sessions; see `plugins/arckit-claude/hooks/README.md`.)
- `keep-coding-instructions: true` (v2.1.94+) — persist instructions across `/compact` for long-running commands
- `disallowed-tools:` (v2.1.152+) — remove tools from the model while the command/skill is active; available but **not yet used** in ArcKit (candidate hardening for read-only commands like `/arckit:search`, `/arckit:navigator`, `/arckit:health`)
- `handoffs:` — list of `{command, description?, condition?}` entries; converter renders these as a "Suggested Next Steps" section for non-Claude targets

Effort, `keep-coding-instructions`, and `disallowed-tools` are stripped by the converter for non-Claude targets.

**Fast mode** (`/fast` toggle) runs Claude Opus with a speed-prioritised API configuration — same model, identical quality, up to 2.5x faster at $10/$50 per MTok (flat across the full 1M context). Supported on **Opus 5 and Opus 4.8 only**; **Opus 4.7 fast mode was removed on 2026-07-24** and Claude Code still treats 4.7 as a fast-mode model in its toggle logic while the API rejects those requests, so a 4.7 session with `/fast` on silently fails rather than degrading to standard speed. As of Claude Code v2.1.219 the default backing model is **Opus 5** (Opus 4.8 on v2.1.154–v2.1.218, Opus 4.7 on v2.1.142–v2.1.153). Requires usage credits and is unavailable on Bedrock, Vertex, Foundry, and Claude Platform on AWS. `/fast` is unrelated to the `effort:` frontmatter — the two compose: a command with `effort: xhigh` still benefits from `/fast` if the user has toggled it.

### Agent System

Some commands delegate to **autonomous agents** (`plugins/arckit-claude/agents/arckit-{name}.md`) that run as subprocesses via the Task tool. Agents are used for commands that perform extensive web research (>10 WebSearch/WebFetch/MCP calls) to keep that context isolated from the main conversation.

| Agent | Command | Purpose |
|-------|---------|---------|
| `arckit-research` | *(non-Claude only)* | Market research, vendor evaluation, build vs buy, TCO |
| `arckit-datascout` | *(non-Claude only)* | Data source discovery, API catalogue search |
| `arckit-aws-research` | *(non-Claude only)* | AWS via AWS Knowledge MCP |
| `arckit-azure-research` | *(non-Claude only)* | Azure via Microsoft Learn MCP |
| `arckit-gcp-research` | *(non-Claude only)* | GCP via Google Developer Knowledge MCP |
| `arckit-framework` | `/arckit:framework` | Transform artifacts into a structured framework |
| `arckit-gov-reuse` | *(non-Claude only)* | Government code reuse via govreposcrape |
| `arckit-gov-code-search` | *(non-Claude only)* | Government code semantic search |
| `arckit-gov-landscape` | *(non-Claude only)* | Government code landscape analysis |
| `arckit-grants` | *(non-Claude only)* | UK government grants and funding research |
| `arckit-tenders` | *(non-Claude only)* | UK procurement award benchmarks via UK Tenders MCP |
| `arckit-competitors` | *(non-Claude only)* | Competitor landscape via UK Tenders MCP |

**Reader/writer subagents** (19 internal, not user-invocable): `research`, `datascout`, `grants`, `gov-reuse`, `gov-code-search`, `gov-landscape`, `tenders` and the three cloud-research commands follow the three-tier orchestrator pattern (`plugins/arckit-claude/docs/READER-PATTERN.md`); `competitors` reuses `arckit-tenders-reader` and ships only a writer. Each ships a `arckit-{name}-reader.md` (web/MCP evidence gathering, returns JSON) and a `arckit-{name}-writer.md` (renders validated payload into artefact, no network tools). **Total: 31 agents** (12 single-tier + 19 reader/writer subagents). The three cloud-research commands share one writer (`arckit-cloud-research-writer`) and one schema, because their templates are structurally identical; each keeps its own reader so a provider's reader can only reach that provider's MCP server.

**The orchestrator is the slash command, not an agent file.** For every split command the dispatch, schema-validation and scoring logic lives in `commands/{name}.md` and runs in the main thread, where `Agent` is reliably available — see READER-PATTERN.md for why this is a deliberate choice rather than a platform limitation. The matching `agents/arckit-{research,datascout,grants,gov-reuse,gov-code-search,gov-landscape,aws-research,azure-research,gcp-research,tenders,competitors}.md` files are the **pre-split monoliths, retained only because `converter.py` replaces a command body wholesale with the agent prompt when generating non-Claude targets** (those runtimes cannot dispatch subagents). Do not delete them; do not treat them as the Claude execution path either. They still register as dispatchable Claude agents, so they must keep a `tools:` allowlist — PR #446 stripped it from all three and nobody noticed for three months (#466). **A split command with no monolith is a shipped regression**: `converter.py` falls through to the orchestrator body and all seven non-Claude extensions inline "dispatch the reader via the `Agent` tool", which none of them can honour. `tenders` and `competitors` were born three-tier in #558 and shipped that way for eleven weeks (#447). `scripts/check-orchestrator-leaks.py` now derives its scope from `commands/` via `converter.py::build_agent_map()`, so a new split command is covered the day it lands.

**Agent frontmatter**: valid fields are `name` (required), `description` (required), `model`, `effort`, `maxTurns`, `tools`, `disallowedTools`, `initialPrompt`, and `experimental` (v2.1.248+; today only `experimental.cacheTtl`, a per-agent prompt-cache TTL of `"5m"` or `"1h"` used when no `subagentPromptCacheTtl` setting is configured — no ArcKit agent sets it). `tools` is an allowlist (only the listed tools are available); `disallowedTools` is a denylist applied first, then the allowlist is resolved against what remains. Heavy-research agents in this plugin use `tools` (allowlist) for prompt-injection hardening — see `plugins/arckit-claude/agents/arckit-research.md` for the canonical shape, including MCP tool naming (`mcp__plugin_arckit_<server>__<tool>` — the bare `mcp__<server>__<tool>` form matches nothing in plugin context). Fields like `color` and `permissionMode` remain invalid in plugin context. Claude-only fields (`effort`, `initialPrompt`, `maxTurns`, `disallowedTools`, `tools`, `experimental`) are stripped by the converter — `experimental` is listed pre-emptively so a future `cacheTtl` cannot leak into the seven non-Claude targets. `scripts/check-agent-frontmatter.py` (CI) asserts every agent declares a `tools:` allowlist, that `agents/` holds nothing but agent files, and that no file carries the alphabetised `description, model, name` block `copy_agent_stripped()` leaves behind when it is accidentally run against the plugin source.

Agents are Claude Code only — Codex/OpenCode/Gemini equivalents inline the full agent prompt.

### Plugin Skills

Skills live in `plugins/arckit-claude/skills/{name}/SKILL.md`. Frontmatter: `name`, `description` (≤1,536 chars), optional `paths:` glob list (v2.1.84+) for auto-activation, optional `disallowed-tools:` (v2.1.152+) to remove tools while the skill is active. The converter strips `paths:` and `disallowed-tools:` for non-Claude targets. During plugin development, `/reload-skills` (v2.1.152+) re-scans skill directories without restarting the session.

| Skill | Type | Purpose |
|-------|------|---------|
| `architecture-workflow` | reference | Onboarding flow — which `/arckit:*` to run and in what order |
| `mermaid-syntax` | reference | Mermaid syntax for `/arckit:diagram` outputs |
| `plantuml-syntax` | reference | PlantUML / C4 syntax |
| `wardley-mapping` | reference | Wardley Map syntax + gameplay/doctrine/climatic patterns |
| `arckit-build` | orchestration | Bulk-build harness — parallel artefact generation, YAML-recipe-driven, resumable. Claude-only (parallel `Agent` dispatch) and **manual-invocation only** (`disable-model-invocation: true`) so a bulk build that commits code is never auto-triggered — run `/arckit:arckit-build`. See `plugins/arckit-claude/skills/arckit-build/`. |

**Skills are grown, not built.** The highest-signal content in a skill is its **Gotchas** section (`## Common Gotchas` / `## Common Syntax Gotchas`). When Claude is led into a repeatable mistake while a skill is active — a syntax footgun, a positioning error, a step done in the wrong order — capture it as a gotcha in that skill rather than only fixing it in the current chat. Keep the body concise (it stays in context across turns and costs recurring tokens), push detailed reference material into the skill's `references/` folder (progressive disclosure), and lead `description` with the key use case. See [the Claude Code skills guide](https://code.claude.com/docs/en/skills).

### Plugin User Configuration

Plugin-level user config is declared in `plugins/arckit-claude/.claude-plugin/plugin.json` under `userConfig` (v2.1.83+). Per-field schema: `title`, `type` (`string`/`number`/`boolean`/`directory`/`file`), `description`, `sensitive: true` (stores in keychain). Current fields:

- `GOOGLE_API_KEY` (sensitive) — `google-developer-knowledge` MCP server
- `DATA_COMMONS_API_KEY` (sensitive) — `datacommons-mcp` MCP server
- `organisation_name` — Document Control headers
- `default_classification` — PUBLIC / OFFICIAL / OFFICIAL-SENSITIVE / SECRET
- `governance_framework` — `UK Gov` or `Generic`

Substitution uses `${user_config.KEY}`. Sensitive values are never substituted into prompt content. The converter rewrites `${user_config.KEY}` → `${KEY}` for non-Claude extensions (which fall back to shell env vars).

**MCP `alwaysLoad`** (v2.1.121): **all six** bundled servers set it — not just the AWS/Azure pair. This is load-bearing, not an optimisation: a deferred (non-`alwaysLoad`) plugin MCP server is **not injected into subagent context**, so only the main agent could reach it via ToolSearch. Every MCP-backed agent therefore needs its server eagerly loaded — `arckit-gcp-research-reader` calls `google-developer-knowledge`, `arckit-datascout-reader` calls `datacommons-mcp`, the `gov-*` readers call `govreposcrape`, and so on. **This became more load-bearing, not less, as commands moved to the reader/writer pattern**: every MCP call now happens inside a subagent by design, so a deferred server would leave the reader with no tools at all. **Do not remove `alwaysLoad` to tidy up a server that fails to connect** — it breaks the agent that depends on it. (See also the tool-name prefix rule: agents must allowlist `mcp__plugin_arckit_<server>__<tool>`; a bare `mcp__<server>__<tool>` matches nothing.)

The two **keyed** servers (`google-developer-knowledge`, `datacommons-mcp`) need `${user_config.*}` API keys. On a keyless session they attempt to connect at startup, fail auth, and are marked **failed** in `/plugin` — the session continues normally, bounded by the connect timeout. This is expected and harmless; there is no way to conditionally omit an MCP server based on whether its config value is set, so a blank key means a failed server, **not** an absent one. Do not document it as "leave blank to disable".

The field is passed through unchanged to non-Claude extensions.

### Plugin Hooks

Hook handlers live in `plugins/arckit-claude/hooks/` and are registered in `plugins/arckit-claude/hooks/hooks.json`. All entries use the **exec form** (`command: "node"` + `args: ["${CLAUDE_PLUGIN_ROOT}/hooks/<name>.mjs"]`, v2.1.139+) so the harness execs the binary directly instead of parsing a shell-quoted command line. PostToolUse entries set `continueOnBlock: true` (v2.1.139+) so the observational `update-manifest`, `provenance-stamp`, and `telemetry` hooks can never derail the user's turn — block semantics are reserved for the genuine gates (`file-protection`, `secret-detection`, `validate-arc-filename`). The `PostCompact` event re-injects project context after `/compact` or auto-compaction (`postcompact-rehydrate.mjs`) so the dynamic filesystem-derived state (`projects/`, ARC artefacts, external policies) isn't lost in the summary — companion to `keep-coding-instructions: true` which preserves static command bodies. Individual hook entries also support an `if:` field (v2.1.85+) using permission rule syntax (e.g. `"Write(/projects/**)"`) to narrow triggering and avoid unnecessary Node spawns.

`session-learner.mjs` (the `Stop` hook) emits an optional end-of-turn **nudge** via `hookSpecificOutput.additionalContext` (v2.1.163+) when this session's commits left a curated traceability-chain gap (e.g. created requirements but no traceability matrix → suggest `/arckit:traceability`). The rule engine is the pure `session-nudge.mjs` (`selectNudge`); it is version-gated against `.arckit/memory/.cc-version` (written by `version-check.mjs` at SessionStart) so it stays silent below v2.1.163 — older clients treat a Stop `additionalContext` as a hook error — and is suppressed on `StopFailure` and when `ARCKIT_NO_NUDGE` is set. See `plugins/arckit-claude/hooks/README.md`.

Platform hook capabilities available but **not yet used** by ArcKit (flagged for future adoption, tracked on #522): `SessionStart` hooks can return `hookSpecificOutput.sessionTitle` to name the session and `reloadSkills: true` to re-scan skill directories (v2.1.152+); and the `MessageDisplay` event (v2.1.152+) can transform or hide assistant text as it is displayed (speculative classification-aware redaction for OFFICIAL-SENSITIVE work).

Notable hooks: provenance stamping, manifest auto-update, session telemetry, secret detection, requirement-graph injection. **For full details on each hook, see [`plugins/arckit-claude/hooks/README.md`](plugins/arckit-claude/hooks/README.md).**

### Plugin Monitors

Background monitors declared via the `experimental.monitors` key in `plugin.json` (v2.1.105+, moved under `experimental` in v2.1.129). Each entry runs as a persistent subprocess; stdout lines are delivered as session notifications. Schema: `name`, `command` (supports `${CLAUDE_PLUGIN_ROOT}` and `${user_config.*}`), `description`, `when` (`always` or `on-skill-invoke:<skill>`). **Needs v2.1.144** — earlier versions used monitor stdout to title new sessions, so the `stale-artifact-scan` description leaked into session names instead of the user's first prompt. (ArcKit's overall min-version floor is **v2.1.234**, set in `version-check.mjs` — the release that stops MCP diagnostics printing resolved secrets, which matters because ArcKit's two keyed MCP servers fail their connections by design on a keyless session. It carries v2.1.221's fix for `WebSearch` 400s at `effort: xhigh`/`max` with thinking disabled, v2.1.222-v2.1.224's permission and sandbox bypass fixes, v2.1.219's Claude Opus 5 availability, v2.1.200's project-scoped plugin worktree loading and `claude agents --plugin-dir` visibility, the v2.1.198+ background-subagent reliability fixes, and the v2.1.172 wildcard-domain `WebFetch` fix.)

Current monitor: `stale-artifact-scan` runs `plugins/arckit-claude/scripts/bash/detect-stale-artifacts.sh` at session start in repos with `projects/`, emitting one line per artefact whose Document Control `Next Review Date` is overdue, or whose status is `DRAFT` and untouched in 14+ days.

Monitors are Claude-only — not propagated to non-Claude extensions.

### Monitor Tool & Remote Control (user-facing)

The built-in `Monitor` tool (v2.1.98+) tails stdout from a background Bash process and delivers each line to Claude as an in-session notification. Useful for long `/arckit:research` or autoresearch runs (redirect to a log file, then `Monitor` the tail).

[Claude Code Remote Control](https://code.claude.com/docs/en/remote-control) (v2.1.110+) lets users drive a local session from claude.ai/code or the mobile app. Combined with `/config → Push when Claude decides`, the phone gets a notification when long-running work finishes. Fits research-heavy commands and overnight autoresearch loops. Pro/Max plans only; Claude Code only.

### Project Structure Created by `arckit init`

```text
project/
├── .arckit/{templates,scripts/bash}/  # Templates + helper scripts
├── .agents/skills/                    # Codex CLI skills ($arckit-*)
├── .codex/{agents,config.toml}/       # Codex CLI agents + MCP/role config
├── .opencode/commands/                # OpenCode CLI commands
├── docs/                              # Unless --minimal
└── projects/
    ├── 000-global/ARC-000-PRIN-v1.0.md
    └── 001-project-name/
        ├── ARC-001-{REQ,STKE,RISK,SOBC}-v1.0.md
        ├── decisions/ARC-001-ADR-001-v1.0.md   # multi-instance
        ├── diagrams/ARC-001-DIAG-001-v1.0.md   # multi-instance
        └── vendors/
```

**Init flags**: `--ai {codex|opencode|copilot}` (`claude` redirects to plugin install, `gemini` to extension install), `--minimal` (skip docs/guides), `--no-git`, `--here`.

### Helper Scripts

`scripts/bash/`:

- `common.sh` — shared utilities (find repo root, slugify, logging) sourced by all others
- `create-project.sh` — creates numbered project dirs, returns JSON
- `check-prerequisites.sh` — validates environment
- `list-projects.sh` — lists projects with artifact counts (table or JSON)
- `migrate-filenames.sh` — migrates files to new naming convention (`--dry-run` supported)

`scripts/generate-document-id.mjs` (in `plugins/arckit-claude/scripts/`): generates IDs like `ARC-001-REQ-v1.0`. Flags: `--filename` (returns the `.md` filename), `--next-num DIR` (next sequential number for a multi-instance type), `--relpath` (prefixes the `SUBDIR_MAP` subdirectory). It **imports** `MULTI_INSTANCE_TYPES`, `KNOWN_TYPES` and `SUBDIR_MAP` from `config/doc-types.mjs`, so there is exactly one copy of each list, and it **rejects an unregistered doc-type code** rather than emitting a name `validate-arc-filename.mjs` will block. The bash version restated the multi-instance list in two hand-maintained copies, which drifted three times and needed a dedicated CI guard; `scripts/bash/generate-document-id.sh` is now a shim that execs the `.mjs` and will be removed a release after #723.

`scripts/converter.py`: config-driven converter using `AGENT_CONFIG` dict — adding a new AI target only requires a new dict entry. Converts plugin commands to Codex Markdown, Codex Skills (directory-per-command with `SKILL.md` + `agents/openai.yaml`), OpenCode Markdown, Gemini extension TOML, and Copilot prompts. Rewrites `${CLAUDE_PLUGIN_ROOT}` paths, copies supporting files, generates Codex `config.toml` (MCP + agent roles), per-agent `.toml` files, and rewrites skill command references.

`scripts/bump-version.sh`, `scripts/generate-release-notes.sh`, `scripts/push-extensions.sh`: see [`docs/RELEASING.md`](docs/RELEASING.md).

## Adding a New Slash Command

1. Create `plugins/arckit-claude/commands/{name}.md` with YAML frontmatter (source of truth for Claude Code)
2. Create `.arckit/templates/{name}-template.md` (also copy to `plugins/arckit-claude/templates/`)
3. Create `docs/guides/{name}.md`, then run `python3 scripts/check-guide-parity.py --sync` to copy it into `plugins/arckit-claude/docs/guides/`
4. If heavy web research (>10 WebSearch/WebFetch calls), create `plugins/arckit-claude/agents/arckit-{name}.md` and make the command a thin wrapper
5. Run `python scripts/converter.py` to generate Codex/OpenCode/Gemini/Copilot variants
6. Test plugin in a test repo, test CLI via `arckit init test --ai codex --no-git`
7. Update README.md, docs/index.html, docs/DEPENDENCY-MATRIX.md, CHANGELOG.md

**Command must**: check prerequisites; use `create-project.sh --json` for project path; read its template; **use Write tool** to save output (avoids 32K output token limit); show only a summary; delegate to agent if research-heavy; declare `handoffs:` for logical next steps.

### Guide Trees (two copies, root is canonical)

User guides live in two places: **`docs/guides/`** (canonical — the published site, plus CLI package data) and **`plugins/arckit-claude/docs/guides/`** (the copy `scripts/converter.py` pushes into all seven generated extensions). They must be byte-identical for every guide present in both.

Nothing guarded this until #675, and the trees drifted silently **in both directions** — the plugin tree lost whole sections (including one that four `*-research.md` guides linked to, shipping a dead anchor to every extension), while the root tree went stale on a doc-type code and a config path. `sync-shared-assets.py` does **not** cover `docs/guides`; `scripts/check-guide-parity.py` does, and CI runs it via `lint-markdown.yml`.

- Edit `docs/guides/{name}.md`, then `python3 scripts/check-guide-parity.py --sync`.
- **Never edit the plugin copy directly** — `--sync` overwrites it from root.
- `--sync` only ever writes to the plugin tree. If the plugin copy holds the correct content, port it into the root copy by hand *first*, or the sync destroys it.
- Root-only guides are fine (community-overlay and maintainer docs deliberately don't ship to extensions); a **plugin-only** guide is an error, because it can never reach the site or the CLI package. Record intentional root-only guides in `ROOT_ONLY_BY_DESIGN` in the script.

### Template Customization

- **Defaults**: `.arckit/templates/` (shipped with ArcKit, refreshed by `arckit init`)
- **User overrides**: `.arckit/templates-custom/` (preserved across updates)

Commands check `templates-custom/` first, fall back to `templates/`. Plugin commands reference templates via `${CLAUDE_PLUGIN_ROOT}/templates/`. When adding/modifying templates, update both `plugins/arckit-claude/templates/` and `.arckit/templates/`.

`/arckit:customize list` lists templates; `/arckit:customize <name>` copies one for editing.

### Document Control Standard

Every template starts with a **Document Control** table followed by **Revision History**.

Standard fields (in order): Document ID, Document Type, Project, Classification (PUBLIC / OFFICIAL / OFFICIAL-SENSITIVE / SECRET), Status (DRAFT / IN_REVIEW / APPROVED / PUBLISHED / SUPERSEDED / ARCHIVED), Version, Created Date, Last Modified, Review Cycle, Next Review Date, Owner, Reviewed By, Approved By, Distribution. Domain-specific fields (e.g., "ADR Number") go after.

**Revision History columns**: Version | Date | Author | Changes | Approved By | Approval Date.

**Standard Footer**:

```markdown
---

**Generated by**: ArcKit `/arckit.{command}` command
**Generated on**: [DATE]
**ArcKit Version**: [VERSION]
**Project**: [PROJECT_NAME]
**Model**: [AI_MODEL]
```

The footer is human-authored. A second machine-stamped block (`## Build Provenance`) is appended automatically by `provenance-stamp.mjs` for artefacts under `projects/**` — see [`plugins/arckit-claude/hooks/README.md`](plugins/arckit-claude/hooks/README.md). Templates do not need to include the provenance block.

## Version Management

CLI and plugin versions are independent; all extensions track the plugin. `scripts/bump-version.sh <version>` updates all 15 version-bearing locations in one go. **For the full release flow, see [`docs/RELEASING.md`](docs/RELEASING.md).**

All changes go through a feature branch and are merged to `main` via PR. Never push directly to `main`.

## Test Repositories

ArcKit maintains public test repos on GitHub (pattern: `arckit-test-project-v*`). All test repos use the plugin via the marketplace; only `projects/`, `docs/`, repo-specific `README.md`/`CLAUDE.md`, and `.devcontainer/` are repo-local. **For the full repo list and plugin-setup details, see [`docs/TEST-REPOS.md`](docs/TEST-REPOS.md).**

## Key Patterns

- **Agent Delegation**: Heavy-research commands (research, datascout, *-research, gov-*, grants) delegate to agents in `plugins/arckit-claude/agents/` via the Task tool. The slash command is a thin wrapper.
- **Token Limit Handling**: Commands MUST use Write tool for large documents to avoid the 32K output token limit.
- **Template-Driven Generation**: Never generate freeform documents — always use templates from `.arckit/templates/`.
- **Traceability Chain**: Stakeholders → Goals → Requirements (BR/FR/NFR/INT/DR) → Data Model → Components → User Stories.
- **Citation Traceability**: When commands read external documents (`external/`, `policies/`, `vendors/`, MCP queries, web fetches), they add inline citation markers (`[DOC_ID-CN]`) and an "External References" section. See `plugins/arckit-claude/references/citation-instructions.md`.
- **Requirement ID Prefixes** (numeric suffix is 1–3 digits; both `BR-1` and `BR-001` are valid):
  - `BR-xxx` Business, `FR-xxx` Functional, `NFR-xxx` Non-Functional (NFR-P/NFR-SEC/etc.), `INT-xxx` Integration, `DR-xxx` Data
- **UK Government Context**: Many commands target UK public sector (GDS Service Standard, TCoP, NCSC CAF, Orange/Green Book, G-Cloud/DOS procurement). See README.md for full compliance coverage.
- **Wardley Map Format**: OnlineWardleyMaps syntax for visualization at <https://create.wardleymaps.ai>.
